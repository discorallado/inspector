<?php

// Sin rutas propias: toda la UI del módulo vive en el panel de Filament
// (ver AdminPanelProvider::discoverResources). Este archivo lo sigue
// cargando RouteServiceProvider::mapWebRoutes(), se queda vacío como
// scaffold estándar de nwidart/laravel-modules por si en algún momento
// se necesita una ruta web fuera de Filament.
